{
  "name": "claude-desktop-mirror",
  "version": "1.0.0",
  "description": "Claude Desktop Mirror for Obsidian",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs --watch",
    "install-plugin": "node esbuild.config.mjs production && node install.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.19.0",
    "obsidian": "^1.4.0",
    "typescript": "^5.0.0"
  }
}
